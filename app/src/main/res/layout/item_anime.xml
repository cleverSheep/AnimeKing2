<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_anime_image"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:contentDescription="@string/anime_image"
            android:imageUrl="@{anime.attributes.coverImage.original ?? anime.attributes.posterImage.original}"
            android:scaleType="center"
            android:src="@mipmap/ic_launcher_round"
            app:layout_constraintDimensionRatio="16:9"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <TextView
            android:id="@+id/tv_anime_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#afff"
            android:fontFamily="@font/bahnscrift"
            android:text="@{anime.attributes.canonicalTitle}"
            android:textAlignment="center"
            android:textColor="@android:color/black"
            android:textSize="15sp"
            app:layout_constraintBottom_toBottomOf="@+id/iv_anime_image"
            app:layout_constraintEnd_toEndOf="@+id/iv_anime_image"
            app:layout_constraintStart_toStartOf="@+id/iv_anime_image"
            tools:text="Title" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <data>

        <variable
            name="anime"
            type="com.murrayde.retrofittesting.model.AnimeData" />
    </data>

</layout>